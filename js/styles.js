//Buisness Logic Testing

describe('bleep converter', () => {
  test('should return bleep if number 1 is present', () => {
    const text = "hello";
    expect(wordCounter(text)).toEqual(1);
  });
});